
  # Sistema de ventas de cloudings

  This is a code bundle for Sistema de ventas de cloudings. The original project is available at https://www.figma.com/design/M7IkmQ4OGZQqPWjprIyain/Sistema-de-ventas-de-cloudings.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  